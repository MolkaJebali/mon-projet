<!DOCTYPE html>
<html>
<head>
    <title>Liste des Auteurs</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Liste des Auteurs</h1>
        
        <a href="{{ path('app_author_new') }}" class="btn btn-primary mb-3">Ajouter un auteur</a>
        <a href="{{ path('app_author_index') }}" class="btn btn-secondary mb-3">Retour à l'accueil</a>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nom</th>
                    <th>Email</th>
                    <th>Nombre de livres</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for author in authors %}
                <tr>
                    <td>{{ author.id }}</td>
                    <td>{{ author.username }}</td>
                    <td>{{ author.email }}</td>
                    <td>{{ author.nbBooks }}</td>  {# CORRIGÉ : nbBooks au lieu de nb_books #}
                    <td>
                        <a href="{{ path('app_author_show', {'id': author.id}) }}" class="btn btn-info btn-sm">Voir</a>
                        <a href="{{ path('app_author_edit', {'id': author.id}) }}" class="btn btn-warning btn-sm">Edit</a>
                        <a href="{{ path('app_author_delete', {'id': author.id}) }}" class="btn btn-danger btn-sm" onclick="return confirm('Êtes-vous sûr?')">Delete</a>
                    </td>
                </tr>
                {% else %}
                <tr>
                    <td colspan="5" class="text-center">Aucun auteur trouvé</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</body>
</html>