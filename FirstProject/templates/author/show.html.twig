<!DOCTYPE html>
<html>
<head>
    <title>Détails de l'Auteur</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-4">
        <h1>Détails de l'Auteur</h1>
        
        {% if author is defined %}
            {# Cas où on passe un objet author (méthode show()) #}
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ author.username }}</h5>
                    <p class="card-text"><strong>Email:</strong> {{ author.email }}</p>
                    <p class="card-text"><strong>Nombre de livres:</strong> {{ author.nbBooks }}</p>
                </div>
            </div>
        {% elseif name is defined %}
            {# Cas où on passe seulement un nom (méthode showAuthor()) #}
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">{{ name }}</h5>
                    <p class="card-text">Informations détaillées non disponibles (affichage par nom uniquement).</p>
                </div>
            </div>
        {% else %}
            <p class="alert alert-warning">Aucune information disponible.</p>
        {% endif %}
        
        <div class="mt-3">
            <a href="{{ path('app_author_list') }}" class="btn btn-secondary">← Retour à la liste</a>
            <a href="{{ path('app_author_index') }}" class="btn btn-outline-primary">Accueil auteurs</a>
        </div>
    </div>
</body>
</html>